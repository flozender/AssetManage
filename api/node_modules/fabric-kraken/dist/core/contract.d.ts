import { Contract } from 'fabric-contract-api';
import KrakenContext from './context';
export default abstract class KrakenContract extends Contract {
    constructor(alias: string);
    createContext(): KrakenContext;
    beforeTransaction(ctx: KrakenContext): Promise<void>;
}
