import { ClientIdentity, X509 } from 'fabric-shim';
export default class AuthorService {
    /** Common Name of the transaction's author. */
    userId: string;
    /** ID of the transaction's author's Membership Service Provider. */
    mspId: string;
    /** Certificate used by the transaction's author to sign the transaction. */
    certificate: X509.Certificate;
    clientIdentity: ClientIdentity;
    constructor(clientIdentity: ClientIdentity);
    /**
     * Parse the ID string that the client identity generates to find the UserID of the certificate
     * @param rawId
     */
    private parseId;
}
